pre-push:
  # Keep local hooks shell-free for Windows lefthook compatibility.
  # Dev-server smoke checks belong in CI where the server lifecycle is controlled.
  parallel: true
  commands:
    typecheck:
      run: bun run check
    lint:
      run: bun run lint
